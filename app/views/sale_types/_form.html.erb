<%= form_with(model: sale_type, local: true) do |form| %>
  <% if sale_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sale_type.errors.count, "error") %> prohibited this sale_type from being saved:</h2>

      <ul>
      <% sale_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :sale_type_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
